import {React} from 'react';
const Footer = () => {
  return (
  <section className="footer">
    <footer className='container d-flex justify-content-end'>Mentions légales</footer>
  </section>);
};

export default Footer;
